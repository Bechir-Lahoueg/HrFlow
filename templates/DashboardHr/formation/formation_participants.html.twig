{% extends 'base.html.twig' %}

{% block title %}RH - Participants de la session{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .formation-session-theme {
            --fp-title: #022E69;
            --fp-primary: #145EB7;
            --fp-accent: #3FA9F5;
            --fp-border: #D0D0D0;
        }
        .fp-table thead th { position: sticky; top: 0; z-index: 10; }
        .fp-row { transition: background-color .2s ease, transform .2s ease; }
        .fp-row:hover { background: #f3f8ff; transform: translateY(-1px); }
        .fp-surface {
            background: #fff;
            border: 1px solid var(--fp-border);
            border-radius: 1rem;
            box-shadow: 0 8px 24px rgba(2, 46, 105, 0.06);
        }
    </style>
{% endblock %}

{% block body %}
<div class="formation-session-theme min-h-screen bg-[#f5f8fd] dot-grid">
    {% include 'DashboardHr/components/_sidebar.html.twig' %}

    <div class="lg:ml-[272px] transition-all duration-300">
        {% include 'DashboardHr/components/_header.html.twig' %}

        {% for message in app.flashes('success') %}
            <div class="mx-6 mt-4 px-4 py-3 rounded-xl bg-emerald-50 border border-emerald-200 text-emerald-700 text-sm font-medium flex items-center gap-2 animate-slide-up">
                <svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="mx-6 mt-4 px-4 py-3 rounded-xl bg-red-50 border border-red-200 text-red-700 text-sm font-medium flex items-center gap-2 animate-slide-up">
                <svg class="w-4 h-4 shrink-0" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 9v3.75m9-.75a9 9 0 11-18 0 9 9 0 0118 0zm-9 3.75h.008v.008H12v-.008z"/></svg>
                {{ message }}
            </div>
        {% endfor %}

        <main class="p-6 space-y-6 max-w-7xl mx-auto">
            <div class="mb-6">
                <a href="{{ path('rh_formation_sessions', {id: formation.id}) }}" class="inline-flex items-center gap-2 text-[#022E69] hover:text-[#145EB7] text-sm font-semibold">
                    <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M15 19l-7-7 7-7"/></svg>
                    Retour aux sessions
                </a>
            </div>

            {# ══════════ Hero Banner ══════════ #}
            <section class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-slate-900 via-sky-900 to-indigo-900 text-white p-8 md:p-10 animate-slide-up">
                <div class="absolute -left-16 bottom-0 h-56 w-56 rounded-full bg-sky-300/15 blur-3xl mesh-blob"></div>
                <div class="absolute -right-16 -top-16 h-56 w-56 rounded-full bg-cyan-300/15 blur-3xl mesh-blob-alt"></div>
                <div class="relative z-10">
                    <div class="flex items-center gap-2 mb-3">
                        <div class="w-2 h-2 rounded-full bg-cyan-400 animate-pulse"></div>
                        <span class="text-[11px] font-bold text-cyan-200 uppercase tracking-wider">RH Control Center</span>
                    </div>
                    <h2 class="text-3xl md:text-4xl font-extrabold leading-tight">Participants à la session</h2>
                    <p class="mt-3 text-[15px] text-slate-300 max-w-xl leading-relaxed">{{ formation.titre }}</p>
                    <div class="mt-4 flex items-center gap-4 text-sm">
                        <span class="bg-white/10 px-3 py-1 rounded-full"><i class="far fa-calendar-alt"></i> {{ session.dateDebut|date('d M Y') }} - {{ session.dateFin|date('d M Y') }}</span>
                        <span class="text-cyan-200"><i class="fas fa-map-marker-alt"></i> {{ session.lieu }}</span>
                        <span class="text-slate-400">•</span>
                        <span class="text-slate-300">Capacité : <span class="font-bold text-white">{{ participations|length }} / {{ session.capaciteMax }}</span></span>
                    </div>
                </div>
            </section>

            <section class="fp-surface p-6 animate-slide-up stagger-5">
                <div class="flex flex-col lg:flex-row lg:items-center lg:justify-between gap-4 mb-5">
                    <div class="flex items-center gap-3">
                        <div class="w-9 h-9 rounded-xl text-white flex items-center justify-center" style="background:linear-gradient(135deg,#145EB7,#3FA9F5)">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M17 20h5V4H2v16h5m10 0v-5H7v5m10 0H7m0 0H2M12 11V7m0 4v3m0-3h3m-3 0H9"/></svg>
                        </div>
                        <h3 class="text-[15px] font-bold text-[#022E69]">Demandes de participation</h3>
                    </div>
                </div>

                <div class="overflow-x-auto rounded-xl border max-h-[70vh]" style="border-color:#D0D0D0;">
                    <table class="fp-table min-w-full text-sm">
                        <thead>
                            <tr class="text-left text-[11px] font-bold uppercase tracking-wider text-[#022E69] border-b" style="border-color:#D0D0D0;background:#edf4ff;">
                                <th class="py-3 px-4">Employé</th>
                                <th class="py-3 px-4">Email</th>
                                <th class="py-3 px-4">Date de demande</th>
                                <th class="py-3 px-4">Statut</th>
                                <th class="py-3 px-4">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                        {% for participation in participations %}
                            <tr class="fp-row border-b align-middle" style="border-color:#eef2f8;">
                                <td class="py-3 px-4">
                                    <div class="flex items-center gap-2.5">
                                        <div class="w-9 h-9 rounded-xl flex items-center justify-center shrink-0" style="background:linear-gradient(135deg,#022E69,#145EB7);">
                                            <span class="text-[11px] font-bold text-white">{{ participation.employee.firstName|first|upper }}{{ participation.employee.lastName|first|upper }}</span>
                                        </div>
                                        <div class="flex items-center gap-2 flex-wrap">
                                            <span class="text-[13px] font-semibold text-[#022E69]">{{ participation.employee.firstName }} {{ participation.employee.lastName }}</span>
                                            {% if participation.employee.rhId == participation.session.formation.rhId %}
                                                <span class="inline-flex items-center gap-1 px-2 py-0.5 rounded-full text-[10px] font-bold bg-[#145EB7]/12 text-[#145EB7] border border-[#145EB7]/25">Prioritaire RH</span>
                                            {% endif %}
                                        </div>
                                    </div>
                                </td>
                                <td class="py-3 px-4 text-zinc-500">{{ participation.employee.email }}</td>
                                <td class="py-3 px-4 font-semibold text-[#022E69]">{{ participation.dateInscription|date('d M Y') }}</td>
                                <td class="py-3 px-4">
                                    <span class="inline-flex items-center gap-1.5 px-2.5 py-1 rounded-full text-[11px] font-bold
                                        {% if participation.statutParticipation == 'Inscrit' %}text-[#145EB7] border{% endif %}
                                        {% if participation.statutParticipation == 'Accepte' %}text-emerald-700 border border-emerald-200 bg-emerald-50{% endif %}
                                        {% if participation.statutParticipation == 'Refuse' %}text-red-700 border border-red-200 bg-red-50{% endif %}
                                        {% if participation.statutParticipation == 'Certificat obtenu' %}text-[#022E69] border{% endif %}"
                                        {% if participation.statutParticipation in ['Inscrit','Certificat obtenu'] %}style="background:#edf4ff;border-color:#bfdbfe;"{% endif %}>
                                        <span class="w-1.5 h-1.5 rounded-full
                                            {% if participation.statutParticipation == 'Inscrit' %}bg-[#3FA9F5]{% endif %}
                                            {% if participation.statutParticipation == 'Accepte' %}bg-emerald-500{% endif %}
                                            {% if participation.statutParticipation == 'Refuse' %}bg-red-500{% endif %}
                                            {% if participation.statutParticipation == 'Certificat obtenu' %}bg-[#145EB7]{% endif %}"></span>
                                        {{ participation.statutParticipation }}
                                    </span>
                                </td>
                                <td class="py-3 px-4">
                                    {% if participation.statutParticipation == 'Inscrit' %}
                                        <div class="flex gap-2">
                                            <form method="post" action="{{ path('rh_formation_participation_approve', {id: participation.id}) }}">
                                                <input type="hidden" name="_token" value="{{ csrf_token('approve-participation-' ~ participation.id) }}">
                                                <button type="submit" class="text-xs font-bold px-3 py-1.5 rounded-md text-white transition-colors flex items-center justify-center gap-1.5" style="background:#145EB7;">
                                                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5"/></svg>
                                                    Accepter
                                                </button>
                                            </form>
                                            <form method="post" action="{{ path('rh_formation_participation_reject', {id: participation.id}) }}">
                                                <input type="hidden" name="_token" value="{{ csrf_token('reject-participation-' ~ participation.id) }}">
                                                <button type="submit" class="text-xs font-bold px-3 py-1.5 rounded-md bg-red-600 text-white hover:bg-red-700 transition-colors flex items-center justify-center gap-1.5">
                                                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
                                                    Refuser
                                                </button>
                                            </form>
                                        </div>
                                    {% else %}
                                        <span class="text-[11px] text-[#022E69] border px-2 py-1 rounded" style="border-color:#D0D0D0;background:#f8fbff;">Action traitée</span>
                                    {% endif %}
                                </td>
                            </tr>
                        {% else %}
                            <tr><td colspan="5" class="py-14 text-center text-zinc-400 text-sm">Aucune demande de participation pour cette session.</td></tr>
                        {% endfor %}
                        </tbody>
                    </table>
                </div>
            </section>
        </main>
    </div>
</div>
{% endblock %}

