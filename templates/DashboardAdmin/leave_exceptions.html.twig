{% extends 'base.html.twig' %}

{% block title %}Conges Exceptionnels | HrFlow{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .decision-row{transition:all .15s ease}
        .decision-row:hover{background:linear-gradient(90deg,#f8fafc,#f1f5f9)}
        .dark .decision-row:hover{background:rgba(255,255,255,.03)}
        .status-pill{display:inline-flex;align-items:center;gap:6px;padding:4px 10px;border-radius:20px;font-size:11px;font-weight:600;letter-spacing:.02em}
        .field-input{transition:all .2s}
        .field-input:focus{box-shadow:0 0 0 3px rgba(6,182,212,.12)}
    </style>
{% endblock %}

{% block body %}
{# Compute stats #}
{% set pendingCount = 0 %}
{% set approvedCount = 0 %}
{% set rejectedCount = 0 %}
{% for leave in leaveRequests %}
    {% if leave.workflowStatus == 'ADMIN_PENDING' %}
        {% set pendingCount = pendingCount + 1 %}
    {% elseif leave.workflowStatus == 'ADMIN_APPROVED' %}
        {% set approvedCount = approvedCount + 1 %}
    {% elseif leave.workflowStatus == 'ADMIN_REJECTED' %}
        {% set rejectedCount = rejectedCount + 1 %}
    {% endif %}
{% endfor %}

<div class="min-h-screen bg-[#f8f9fb] dark:bg-zinc-950 dot-grid">
    {% include 'DashboardAdmin/components/_sidebar.html.twig' %}

    <div class="lg:ml-[272px] transition-all duration-300">
        {% include 'DashboardAdmin/components/_header.html.twig' %}

        {% for message in app.flashes('success') %}
            <div class="mx-6 lg:mx-8 mt-4 px-4 py-3 rounded-xl bg-emerald-50 dark:bg-emerald-500/10 border border-emerald-200 dark:border-emerald-500/30 text-emerald-700 dark:text-emerald-400 text-sm font-medium flex items-center gap-2.5 animate-slide-up">
                <div class="w-6 h-6 rounded-full bg-emerald-100 dark:bg-emerald-500/20 flex items-center justify-center shrink-0">
                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5"/></svg>
                </div>
                {{ message }}
            </div>
        {% endfor %}
        {% for message in app.flashes('error') %}
            <div class="mx-6 lg:mx-8 mt-4 px-4 py-3 rounded-xl bg-rose-50 dark:bg-rose-500/10 border border-rose-200 dark:border-rose-500/30 text-rose-700 dark:text-rose-400 text-sm font-medium flex items-center gap-2.5 animate-slide-up">
                <div class="w-6 h-6 rounded-full bg-rose-100 dark:bg-rose-500/20 flex items-center justify-center shrink-0">
                    <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
                </div>
                {{ message }}
            </div>
        {% endfor %}

        <main class="p-6 lg:p-8 space-y-6 max-w-[1440px] mx-auto"
              data-controller="tab-switcher"
              data-tab-switcher-active-value="0">

            {# ═══════════ HERO BANNER ═══════════ #}
            <section class="relative overflow-hidden rounded-2xl bg-gradient-to-br from-slate-900 via-violet-950 to-purple-900 text-white p-8 md:p-10 animate-slide-up">
                <div class="absolute -left-16 bottom-0 h-56 w-56 rounded-full bg-violet-400/15 blur-3xl mesh-blob"></div>
                <div class="absolute -right-16 -top-16 h-56 w-56 rounded-full bg-purple-300/15 blur-3xl mesh-blob-alt"></div>
                <div class="absolute top-1/2 left-1/3 h-32 w-32 rounded-full bg-fuchsia-400/10 blur-3xl"></div>

                <div class="relative z-10 grid lg:grid-cols-[1fr_1fr] gap-8 items-center">
                    <div>
                        <div class="flex items-center gap-2 mb-3">
                            <div class="w-2 h-2 rounded-full bg-violet-400 animate-pulse"></div>
                            <span class="text-[11px] font-bold text-violet-200 uppercase tracking-[0.15em]">Validation Admin</span>
                        </div>
                        <h2 class="text-3xl md:text-5xl font-display leading-[1.1] tracking-tight">Conges Exceptionnels</h2>
                        <p class="mt-3 text-[14px] text-slate-300/90 max-w-lg leading-relaxed">Validation finale des demandes exceptionnelles apres pre-approbation RH. Gerez les decisions et consultez l'historique.</p>
                    </div>
                    <div class="grid grid-cols-2 sm:grid-cols-4 gap-3">
                        <div class="rounded-xl border border-white/15 bg-white/[0.08] backdrop-blur-sm p-4 hover:bg-white/[0.12] transition-colors group">
                            <p class="text-[9px] uppercase tracking-[0.15em] text-violet-200/60 font-bold">Total</p>
                            <p class="mt-1 text-2xl font-extrabold font-mono-num group-hover:scale-105 transition-transform origin-left">{{ leaveRequests|length }}</p>
                        </div>
                        <div class="rounded-xl border border-white/15 bg-white/[0.08] backdrop-blur-sm p-4 hover:bg-white/[0.12] transition-colors group">
                            <div class="flex items-center justify-between">
                                <p class="text-[9px] uppercase tracking-[0.15em] text-violet-200/60 font-bold">Attente</p>
                                {% if pendingCount > 0 %}<span class="w-2 h-2 rounded-full bg-amber-400 animate-pulse"></span>{% endif %}
                            </div>
                            <p class="mt-1 text-2xl font-extrabold font-mono-num text-amber-300 group-hover:scale-105 transition-transform origin-left">{{ pendingCount }}</p>
                        </div>
                        <div class="rounded-xl border border-white/15 bg-white/[0.08] backdrop-blur-sm p-4 hover:bg-white/[0.12] transition-colors group">
                            <p class="text-[9px] uppercase tracking-[0.15em] text-violet-200/60 font-bold">Approuves</p>
                            <p class="mt-1 text-2xl font-extrabold font-mono-num text-emerald-400 group-hover:scale-105 transition-transform origin-left">{{ approvedCount }}</p>
                        </div>
                        <div class="rounded-xl border border-white/15 bg-white/[0.08] backdrop-blur-sm p-4 hover:bg-white/[0.12] transition-colors group">
                            <p class="text-[9px] uppercase tracking-[0.15em] text-violet-200/60 font-bold">Refuses</p>
                            <p class="mt-1 text-2xl font-extrabold font-mono-num text-rose-400 group-hover:scale-105 transition-transform origin-left">{{ rejectedCount }}</p>
                        </div>
                    </div>
                </div>
            </section>

            {# ═══════════ TAB NAVIGATION (2 tabs) ═══════════ #}
            <div class="animate-slide-up stagger-1 overflow-x-auto pb-1">
                <div class="tab-bar">
                    <div data-tab-switcher-target="indicator" class="tab-indicator"></div>

                    <button type="button" data-tab-switcher-target="tab" data-action="click->tab-switcher#switch" data-index="0" class="tab-btn tab-active" role="tab" aria-selected="true">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/></svg>
                        Decisions
                        {% if pendingCount > 0 %}
                            <span class="tab-count">{{ pendingCount }}</span>
                        {% endif %}
                    </button>

                    <button type="button" data-tab-switcher-target="tab" data-action="click->tab-switcher#switch" data-index="1" class="tab-btn tab-inactive" role="tab" aria-selected="false">
                        <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3.75 6A2.25 2.25 0 016 3.75h2.25A2.25 2.25 0 0110.5 6v2.25a2.25 2.25 0 01-2.25 2.25H6a2.25 2.25 0 01-2.25-2.25V6zM3.75 15.75A2.25 2.25 0 016 13.5h2.25a2.25 2.25 0 012.25 2.25V18a2.25 2.25 0 01-2.25 2.25H6A2.25 2.25 0 013.75 18v-2.25zM13.5 6a2.25 2.25 0 012.25-2.25H18A2.25 2.25 0 0120.25 6v2.25A2.25 2.25 0 0118 10.5h-2.25a2.25 2.25 0 01-2.25-2.25V6z"/></svg>
                        Apercu
                    </button>
                </div>
            </div>

            {# ══════════════════════════════════════════════ #}
            {# ═══════════ PANEL 0: DECISIONS ═════════════ #}
            {# ══════════════════════════════════════════════ #}
            <div data-tab-switcher-target="panel" class="space-y-6">
                <section class="bg-white dark:bg-zinc-900 rounded-2xl border border-zinc-100 dark:border-zinc-800/50 shadow-[0_1px_3px_rgba(0,0,0,.04)] overflow-hidden animate-slide-up">
                    {# Table Header #}
                    <div class="px-6 py-4 border-b border-zinc-100 dark:border-zinc-800/50">
                        <div class="flex items-center justify-between">
                            <div class="flex items-center gap-3">
                                <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-violet-500 to-purple-600 flex items-center justify-center shadow-sm">
                                    <svg class="w-4 h-4 text-white" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/></svg>
                                </div>
                                <div>
                                    <h3 class="text-[15px] font-bold text-zinc-800 dark:text-zinc-200">Validation Admin</h3>
                                    <p class="text-[11px] text-zinc-400">Demandes en attente + historique de vos decisions admin</p>
                                </div>
                            </div>
                            <div class="flex items-center gap-1.5 px-2.5 py-1 rounded-lg bg-violet-50 dark:bg-violet-950/30 border border-violet-100 dark:border-violet-900/50">
                                <div class="w-1.5 h-1.5 rounded-full bg-violet-500 animate-pulse"></div>
                                <span class="text-[10px] font-bold text-violet-700 dark:text-violet-400 uppercase tracking-wider">{{ pendingCount }} en attente</span>
                            </div>
                        </div>
                    </div>

                    {# Table #}
                    <div class="overflow-x-auto">
                        <table class="min-w-full">
                            <thead>
                                <tr class="border-b border-zinc-100 dark:border-zinc-800/50">
                                    <th class="text-left text-[10px] font-semibold uppercase tracking-[0.1em] text-zinc-400 py-3 px-5 bg-zinc-50/60 dark:bg-zinc-800/30">Employe</th>
                                    <th class="text-left text-[10px] font-semibold uppercase tracking-[0.1em] text-zinc-400 py-3 px-4 bg-zinc-50/60 dark:bg-zinc-800/30">Periode</th>
                                    <th class="text-center text-[10px] font-semibold uppercase tracking-[0.1em] text-zinc-400 py-3 px-4 bg-zinc-50/60 dark:bg-zinc-800/30">Jours</th>
                                    <th class="text-left text-[10px] font-semibold uppercase tracking-[0.1em] text-zinc-400 py-3 px-4 bg-zinc-50/60 dark:bg-zinc-800/30">Urgence</th>
                                    <th class="text-left text-[10px] font-semibold uppercase tracking-[0.1em] text-zinc-400 py-3 px-4 bg-zinc-50/60 dark:bg-zinc-800/30">Justificatif</th>
                                    <th class="text-left text-[10px] font-semibold uppercase tracking-[0.1em] text-zinc-400 py-3 px-4 bg-zinc-50/60 dark:bg-zinc-800/30">Commentaire RH</th>
                                    <th class="text-left text-[10px] font-semibold uppercase tracking-[0.1em] text-zinc-400 py-3 px-4 bg-zinc-50/60 dark:bg-zinc-800/30">Details</th>
                                    <th class="text-left text-[10px] font-semibold uppercase tracking-[0.1em] text-zinc-400 py-3 px-4 bg-zinc-50/60 dark:bg-zinc-800/30">Decision</th>
                                </tr>
                            </thead>
                            <tbody class="divide-y divide-zinc-50 dark:divide-zinc-800/30">
                            {% for leave in leaveRequests %}
                                <tr class="decision-row align-top">
                                    <td class="py-3.5 px-5">
                                        <div class="flex items-center gap-2.5">
                                            <div class="w-8 h-8 rounded-lg bg-gradient-to-br from-violet-500 to-purple-500 flex items-center justify-center shrink-0 shadow-sm">
                                                <span class="text-[11px] font-bold text-white">{{ leave.employeeName|first|upper }}</span>
                                            </div>
                                            <span class="text-[13px] font-semibold text-zinc-700 dark:text-zinc-300">{{ leave.employeeName }}</span>
                                        </div>
                                    </td>
                                    <td class="py-3.5 px-4">
                                        <p class="text-[13px] font-medium text-zinc-700 dark:text-zinc-300">{{ leave.startDate|date('d/m/Y') }}</p>
                                        <p class="text-[11px] text-zinc-400">au {{ leave.endDate|date('d/m/Y') }}</p>
                                    </td>
                                    <td class="py-3.5 px-4 text-center">
                                        <span class="text-[14px] font-bold text-zinc-800 dark:text-zinc-200 tabular-nums">{{ leave.daysCount }}</span>
                                    </td>
                                    <td class="py-3.5 px-4">
                                        {% if leave.urgencyLevel == 'HIGH' %}
                                            <span class="status-pill bg-red-50 dark:bg-red-950/30 text-red-700 dark:text-red-400 border border-red-200 dark:border-red-800/50">
                                                <span class="w-1.5 h-1.5 rounded-full bg-red-500"></span>HIGH
                                            </span>
                                        {% elseif leave.urgencyLevel == 'MEDIUM' %}
                                            <span class="status-pill bg-amber-50 dark:bg-amber-950/30 text-amber-700 dark:text-amber-400 border border-amber-200 dark:border-amber-800/50">
                                                <span class="w-1.5 h-1.5 rounded-full bg-amber-500"></span>MEDIUM
                                            </span>
                                        {% else %}
                                            <span class="status-pill bg-slate-100 dark:bg-slate-800 text-slate-600 dark:text-slate-400 border border-slate-200 dark:border-slate-700">
                                                <span class="w-1.5 h-1.5 rounded-full bg-slate-400"></span>{{ leave.urgencyLevel ?: 'LOW' }}
                                            </span>
                                        {% endif %}
                                    </td>
                                    <td class="py-3.5 px-4">
                                        {% if leave.attachmentPath %}
                                            <a href="{{ leave.attachmentPath }}" target="_blank" class="inline-flex items-center gap-1.5 text-[11px] font-semibold text-violet-600 dark:text-violet-400 hover:text-violet-700 bg-violet-50 dark:bg-violet-950/30 border border-violet-200 dark:border-violet-800/50 px-2.5 py-1.5 rounded-lg transition-colors">
                                                <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M18.375 12.739l-7.693 7.693a4.5 4.5 0 01-6.364-6.364l10.94-10.94A3 3 0 1119.5 7.372L8.552 18.32m.009-.01l-.01.01m5.699-9.941l-7.81 7.81a1.5 1.5 0 002.112 2.13"/></svg>
                                                Ouvrir
                                            </a>
                                        {% else %}
                                            <span class="text-zinc-300 dark:text-zinc-600">-</span>
                                        {% endif %}
                                    </td>
                                    <td class="py-3.5 px-4">
                                        {% if leave.rhComment %}
                                            <p class="text-[12px] text-zinc-500 dark:text-zinc-400 max-w-[180px] truncate" title="{{ leave.rhComment }}">{{ leave.rhComment }}</p>
                                        {% else %}
                                            <span class="text-zinc-300 dark:text-zinc-600">-</span>
                                        {% endif %}
                                    </td>
                                    <td class="py-3.5 px-4">
                                        <button
                                            type="button"
                                            class="leave-detail-btn inline-flex items-center gap-1.5 text-[11px] font-semibold text-slate-700 dark:text-slate-200 bg-slate-100 dark:bg-slate-800 border border-slate-200 dark:border-slate-700 px-2.5 py-1.5 rounded-lg hover:bg-slate-200 dark:hover:bg-slate-700 transition-colors"
                                            data-id="{{ leave.id }}"
                                            data-employee-name="{{ leave.employeeName|e('html_attr') }}"
                                            data-employee-email="{{ leave.employee ? leave.employee.email|default('-') : '-' }}"
                                            data-leave-type="{{ leave.leaveType|default('-')|e('html_attr') }}"
                                            data-request-category="{{ leave.requestCategory|default('-')|e('html_attr') }}"
                                            data-status="{{ leave.status|default('-')|e('html_attr') }}"
                                            data-workflow-status="{{ leave.workflowStatus|default('-')|e('html_attr') }}"
                                            data-start-date="{{ leave.startDate ? leave.startDate|date('d/m/Y') : '-' }}"
                                            data-end-date="{{ leave.endDate ? leave.endDate|date('d/m/Y') : '-' }}"
                                            data-days-count="{{ leave.daysCount|default('-') }}"
                                            data-request-date="{{ leave.requestDate ? leave.requestDate|date('d/m/Y') : '-' }}"
                                            data-urgency="{{ leave.urgencyLevel|default('LOW')|e('html_attr') }}"
                                            data-reason="{{ leave.reason|default('-')|e('html_attr') }}"
                                            data-rh-comment="{{ leave.rhComment|default('-')|e('html_attr') }}"
                                            data-rh-decision-by="{{ leave.rhDecisionBy|default('-')|e('html_attr') }}"
                                            data-rh-decision-at="{{ leave.rhDecisionAt ? leave.rhDecisionAt|date('d/m/Y H:i') : '-' }}"
                                            data-admin-comment="{{ leave.adminComment|default('-')|e('html_attr') }}"
                                            data-admin-decision-by="{{ leave.adminDecisionBy|default('-')|e('html_attr') }}"
                                            data-admin-decision-at="{{ leave.adminDecisionAt ? leave.adminDecisionAt|date('d/m/Y H:i') : '-' }}"
                                            data-expected-return-date="{{ leave.expectedReturnDate ? leave.expectedReturnDate|date('d/m/Y') : '-' }}"
                                            data-attachment-path="{{ leave.attachmentPath|default('')|e('html_attr') }}"
                                            data-attachment-ocr-text="{{ leave.attachmentOcrText|default('-')|e('html_attr') }}"
                                            data-attachment-ocr-summary="{{ leave.attachmentOcrSummary|default('-')|e('html_attr') }}"
                                            data-audit-log="{{ leave.auditLog|default('-')|e('html_attr') }}"
                                        >
                                            <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M2.25 12s3.75-7.5 9.75-7.5 9.75 7.5 9.75 7.5-3.75 7.5-9.75 7.5S2.25 12 2.25 12z"/><path stroke-linecap="round" stroke-linejoin="round" d="M12 15.75a3.75 3.75 0 100-7.5 3.75 3.75 0 000 7.5z"/></svg>
                                            Voir
                                        </button>
                                    </td>
                                    <td class="py-3.5 px-4">
                                        {% if leave.workflowStatus == 'ADMIN_PENDING' %}
                                            <div class="inline-dec"
                                                 data-approve-url="{{ path('app_admin_leave_exception_approve', {id: leave.id}) }}"
                                                 data-approve-token="{{ csrf_token('admin_leave_exception_approve_' ~ leave.id) }}"
                                                 data-approve-label="Approbation finale"
                                                 data-reject-url="{{ path('app_admin_leave_exception_reject', {id: leave.id}) }}"
                                                    data-reject-token="{{ csrf_token('admin_leave_exception_reject_' ~ leave.id) }}"
                                                    data-ai-url="{{ path('app_admin_leave_exception_ai_comment') }}"
                                                    data-ai-token="{{ csrf_token('admin_leave_exception_ai_comment') }}"
                                                    data-ai-leave-id="{{ leave.id }}">
                                                {# Etape 0 : bouton Decider #}
                                                <div class="dec-s0">
                                                    <button type="button" onclick="decS1(this.closest('.inline-dec'))" class="inline-flex items-center gap-1.5 px-2.5 py-1.5 rounded-lg text-[11px] font-bold border border-zinc-300 dark:border-zinc-600 text-zinc-700 dark:text-zinc-300 bg-white dark:bg-zinc-800 hover:bg-zinc-50 dark:hover:bg-zinc-700 transition-all shadow-sm">
                                                        <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M8.25 15L12 18.75 15.75 15m-7.5-6L12 5.25 15.75 9"/></svg>
                                                        Decider
                                                    </button>
                                                </div>
                                                {# Etape 1 : choisir Accepter ou Refuser #}
                                                <div class="dec-s1 hidden">
                                                    <div class="flex items-center gap-1.5 flex-wrap">
                                                        <button type="button" onclick="decS2(this.closest('.inline-dec'),'approve')" class="inline-flex items-center gap-1 px-2.5 py-1.5 rounded-lg text-[11px] font-bold bg-emerald-600 text-white hover:bg-emerald-700 transition-all active:scale-[0.97]">
                                                            <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M4.5 12.75l6 6 9-13.5"/></svg>
                                                            Accepter
                                                        </button>
                                                        <button type="button" onclick="decS2(this.closest('.inline-dec'),'reject')" class="inline-flex items-center gap-1 px-2.5 py-1.5 rounded-lg text-[11px] font-bold bg-rose-600 text-white hover:bg-rose-700 transition-all active:scale-[0.97]">
                                                            <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
                                                            Refuser
                                                        </button>
                                                        <button type="button" onclick="decReset(this.closest('.inline-dec'))" class="text-[10px] text-zinc-400 hover:text-zinc-600 dark:hover:text-zinc-300 underline transition-colors">annuler</button>
                                                    </div>
                                                </div>
                                                {# Etape 2a : formulaire approbation #}
                                                <form class="dec-s2a hidden space-y-1.5" method="post" action="">
                                                    <input type="hidden" name="_token">
                                                    <input type="text" name="admin_comment" placeholder="Commentaire (optionnel)" class="w-full rounded-lg border border-emerald-200 dark:border-emerald-800/50 text-[11px] bg-white dark:bg-zinc-800 py-1.5 px-2.5 focus:outline-none focus:ring-1 focus:ring-emerald-400 placeholder:text-zinc-300 dark:placeholder:text-zinc-600 text-zinc-700 dark:text-zinc-300 min-w-[160px]">
                                                    <button type="button" onclick="decAi(this.closest('.inline-dec'),'approve')" class="w-full inline-flex items-center justify-center gap-1.5 px-2.5 py-1.5 rounded-lg text-[10px] font-bold border border-cyan-200 dark:border-cyan-800/50 text-cyan-700 dark:text-cyan-300 bg-cyan-50 dark:bg-cyan-950/20 hover:bg-cyan-100 dark:hover:bg-cyan-950/40 transition-colors">
                                                        Rediger avec IA
                                                    </button>
                                                    <p class="dec-ai-notice hidden text-[10px] text-zinc-400">Texte suggere par IA, a verifier avant envoi.</p>
                                                    <div class="flex gap-1.5">
                                                        <button type="submit" class="inline-flex items-center gap-1 px-2.5 py-1.5 rounded-lg text-[11px] font-bold bg-emerald-600 text-white hover:bg-emerald-700 transition-all active:scale-[0.97] flex-1 justify-center">
                                                            <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                                                            <span class="dec-approve-label">Approbation finale</span>
                                                        </button>
                                                        <button type="button" onclick="decReset(this.closest('.inline-dec'))" class="px-2 py-1.5 rounded-lg text-[10px] text-zinc-400 hover:text-zinc-600 border border-zinc-200 dark:border-zinc-700 hover:bg-zinc-50 dark:hover:bg-zinc-800 transition-colors">&#8617;</button>
                                                    </div>
                                                </form>
                                                {# Etape 2r : formulaire refus #}
                                                <form class="dec-s2r hidden space-y-1.5" method="post" action="" novalidate>
                                                    <input type="hidden" name="_token">
                                                    <input type="text" name="admin_comment" placeholder="Motif de refus (obligatoire)" class="w-full rounded-lg border border-rose-200 dark:border-rose-800/50 text-[11px] bg-white dark:bg-zinc-800 py-1.5 px-2.5 focus:outline-none focus:ring-1 focus:ring-rose-400 placeholder:text-zinc-300 dark:placeholder:text-zinc-600 text-zinc-700 dark:text-zinc-300 min-w-[160px]">
                                                    <button type="button" onclick="decAi(this.closest('.inline-dec'),'reject')" class="w-full inline-flex items-center justify-center gap-1.5 px-2.5 py-1.5 rounded-lg text-[10px] font-bold border border-cyan-200 dark:border-cyan-800/50 text-cyan-700 dark:text-cyan-300 bg-cyan-50 dark:bg-cyan-950/20 hover:bg-cyan-100 dark:hover:bg-cyan-950/40 transition-colors">
                                                        Rediger avec IA
                                                    </button>
                                                    <p class="dec-ai-notice hidden text-[10px] text-zinc-400">Texte suggere par IA, a verifier avant envoi.</p>
                                                    <div class="flex gap-1.5">
                                                        <button type="submit" class="inline-flex items-center gap-1 px-2.5 py-1.5 rounded-lg text-[11px] font-bold bg-rose-600 text-white hover:bg-rose-700 transition-all active:scale-[0.97] flex-1 justify-center">
                                                            <svg class="w-3 h-3" fill="none" stroke="currentColor" stroke-width="2.5" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
                                                            Refuser
                                                        </button>
                                                        <button type="button" onclick="decReset(this.closest('.inline-dec'))" class="px-2 py-1.5 rounded-lg text-[10px] text-zinc-400 hover:text-zinc-600 border border-zinc-200 dark:border-zinc-700 hover:bg-zinc-50 dark:hover:bg-zinc-800 transition-colors">&#8617;</button>
                                                    </div>
                                                </form>
                                            </div>
                                        {% else %}
                                            <div class="inline-flex items-center gap-1.5 text-[11px] font-semibold text-emerald-700 dark:text-emerald-400">
                                                <svg class="w-3.5 h-3.5" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                                                Approuve
                                            </div>
                                        {% endif %}
                                    </td>
                                </tr>
                            {% else %}
                                <tr>
                                    <td colspan="8" class="py-20 text-center">
                                        <svg class="w-14 h-14 text-zinc-200 dark:text-zinc-700 mx-auto mb-3" fill="none" stroke="currentColor" stroke-width="1" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75m-3-7.036A11.959 11.959 0 013.598 6 11.99 11.99 0 003 9.749c0 5.592 3.824 10.29 9 11.623 5.176-1.332 9-6.03 9-11.622 0-1.31-.21-2.571-.598-3.751h-.152c-3.196 0-6.1-1.248-8.25-3.285z"/></svg>
                                        <p class="text-sm font-medium text-zinc-400 dark:text-zinc-500">Aucune demande exceptionnelle en attente</p>
                                        <p class="text-xs text-zinc-300 dark:text-zinc-600 mt-1">Les demandes pre-approuvees par le RH apparaitront ici</p>
                                    </td>
                                </tr>
                            {% endfor %}
                            </tbody>
                        </table>
                    </div>
                </section>
            </div>

            {# ══════════════════════════════════════════════ #}
            {# ═══════════ PANEL 1: APERCU ════════════════ #}
            {# ══════════════════════════════════════════════ #}
            <div data-tab-switcher-target="panel" class="space-y-6 hidden">
                {# Stat Cards #}
                <div class="grid grid-cols-2 lg:grid-cols-4 gap-4">
                    <div class="card-surface stat-stripe stat-stripe-sky p-5 group">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-10 h-10 rounded-xl bg-sky-50 dark:bg-sky-950/30 flex items-center justify-center group-hover:scale-110 transition-transform">
                                <svg class="w-5 h-5 text-sky-600 dark:text-sky-400" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M3.75 12h16.5m-16.5 3.75h16.5M3.75 19.5h16.5M5.625 4.5h12.75a1.875 1.875 0 010 3.75H5.625a1.875 1.875 0 010-3.75z"/></svg>
                            </div>
                        </div>
                        <p class="text-3xl font-extrabold text-zinc-900 dark:text-zinc-100 font-mono-num leading-none">{{ leaveRequests|length }}</p>
                        <p class="text-[11px] text-zinc-400 mt-2 font-semibold">Toutes les demandes</p>
                    </div>
                    <div class="card-surface stat-stripe stat-stripe-amber p-5 group">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-10 h-10 rounded-xl bg-amber-50 dark:bg-amber-950/30 flex items-center justify-center group-hover:scale-110 transition-transform">
                                <svg class="w-5 h-5 text-amber-600 dark:text-amber-400" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M12 6v6h4.5m4.5 0a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                            </div>
                            {% if pendingCount > 0 %}<span class="w-2.5 h-2.5 rounded-full bg-amber-500 badge-glow-amber"></span>{% endif %}
                        </div>
                        <p class="text-3xl font-extrabold text-amber-600 dark:text-amber-400 font-mono-num leading-none">{{ pendingCount }}</p>
                        <p class="text-[11px] text-zinc-400 mt-2 font-semibold">Actions prioritaires</p>
                    </div>
                    <div class="card-surface stat-stripe stat-stripe-emerald p-5 group">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-10 h-10 rounded-xl bg-emerald-50 dark:bg-emerald-950/30 flex items-center justify-center group-hover:scale-110 transition-transform">
                                <svg class="w-5 h-5 text-emerald-600 dark:text-emerald-400" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M9 12.75L11.25 15 15 9.75M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/></svg>
                            </div>
                        </div>
                        <p class="text-3xl font-extrabold text-emerald-600 dark:text-emerald-400 font-mono-num leading-none">{{ approvedCount }}</p>
                        <p class="text-[11px] text-zinc-400 mt-2 font-semibold">Demandes approuvees</p>
                    </div>
                    <div class="card-surface stat-stripe stat-stripe-rose p-5 group">
                        <div class="flex items-center justify-between mb-4">
                            <div class="w-10 h-10 rounded-xl bg-rose-50 dark:bg-rose-950/30 flex items-center justify-center group-hover:scale-110 transition-transform">
                                <svg class="w-5 h-5 text-rose-600 dark:text-rose-400" fill="none" stroke="currentColor" stroke-width="1.8" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
                            </div>
                        </div>
                        <p class="text-3xl font-extrabold text-rose-600 dark:text-rose-400 font-mono-num leading-none">{{ rejectedCount }}</p>
                        <p class="text-[11px] text-zinc-400 mt-2 font-semibold">Demandes refusees</p>
                    </div>
                </div>
            </div>

        </main>

        {# ═══════════ DETAIL MODAL (outside main to avoid tab-switcher clipping) ═══════════ #}
        <div id="leave-detail-modal" class="hidden fixed inset-0 z-[9999]">
            <div id="leave-detail-backdrop" class="absolute inset-0 bg-zinc-950/60 backdrop-blur-sm"></div>
            <div class="absolute inset-0 flex items-center justify-center p-4">
                <div class="w-full max-w-4xl rounded-2xl border border-zinc-200 dark:border-zinc-700 bg-white dark:bg-zinc-900 shadow-2xl max-h-[90vh] overflow-hidden">
                    <div class="flex items-center justify-between px-6 py-4 border-b border-zinc-100 dark:border-zinc-800">
                        <div>
                            <h3 class="text-lg font-extrabold text-zinc-900 dark:text-zinc-100">Details Conge Exceptionnel</h3>
                            <p class="text-[12px] text-zinc-500">Vue complete de la demande pour validation admin.</p>
                        </div>
                        <button type="button" id="leave-detail-close" class="inline-flex items-center justify-center w-9 h-9 rounded-lg border border-zinc-200 dark:border-zinc-700 text-zinc-500 hover:text-zinc-900 dark:hover:text-zinc-100 hover:bg-zinc-50 dark:hover:bg-zinc-800 transition-colors">
                            <svg class="w-4 h-4" fill="none" stroke="currentColor" stroke-width="2" viewBox="0 0 24 24"><path stroke-linecap="round" stroke-linejoin="round" d="M6 18L18 6M6 6l12 12"/></svg>
                        </button>
                    </div>

                        <div class="p-6 overflow-y-auto max-h-[calc(90vh-80px)] grid grid-cols-1 lg:grid-cols-2 gap-4 text-sm">
                            <div class="rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider mb-3">Demande</p>
                                <div class="space-y-2">
                                    <p><span class="text-zinc-500">ID:</span> <span id="ld-id" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Employe:</span> <span id="ld-employee" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Email:</span> <span id="ld-employee-email" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Type:</span> <span id="ld-type" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Categorie:</span> <span id="ld-category" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Statut:</span> <span id="ld-status" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Workflow:</span> <span id="ld-workflow" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                </div>
                            </div>

                            <div class="rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider mb-3">Periode</p>
                                <div class="space-y-2">
                                    <p><span class="text-zinc-500">Date debut:</span> <span id="ld-start" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Date fin:</span> <span id="ld-end" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Jours ouvrables:</span> <span id="ld-days" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Date demande:</span> <span id="ld-request-date" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Retour prevu:</span> <span id="ld-return-date" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Urgence:</span> <span id="ld-urgency" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                </div>
                            </div>

                            <div class="lg:col-span-2 rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider mb-2">Motif / Justificatif texte</p>
                                <p id="ld-reason" class="text-[13px] text-zinc-700 dark:text-zinc-300 whitespace-pre-wrap break-words">-</p>
                            </div>

                            <div class="rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider mb-3">Decision RH</p>
                                <div class="space-y-2">
                                    <p><span class="text-zinc-500">Commentaire RH:</span> <span id="ld-rh-comment" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Par:</span> <span id="ld-rh-by" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Date:</span> <span id="ld-rh-at" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                </div>
                            </div>

                            <div class="rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider mb-3">Decision Admin</p>
                                <div class="space-y-2">
                                    <p><span class="text-zinc-500">Commentaire Admin:</span> <span id="ld-admin-comment" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Par:</span> <span id="ld-admin-by" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                    <p><span class="text-zinc-500">Date:</span> <span id="ld-admin-at" class="font-semibold text-zinc-800 dark:text-zinc-200">-</span></p>
                                </div>
                            </div>

                            <div class="lg:col-span-2 rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <div class="flex items-center justify-between gap-3 mb-2">
                                    <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider">Justificatif Fichier</p>
                                    <a id="ld-attachment-link" href="#" target="_blank" class="hidden inline-flex items-center gap-1.5 text-[11px] font-semibold text-violet-700 dark:text-violet-400 bg-violet-50 dark:bg-violet-950/30 border border-violet-200 dark:border-violet-800/50 px-2.5 py-1.5 rounded-lg">
                                        Ouvrir le fichier
                                    </a>
                                </div>
                                <p id="ld-attachment-empty" class="text-[12px] text-zinc-400">Aucun fichier joint.</p>
                            </div>

                            <div class="lg:col-span-2 rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider mb-2">Texte extrait (OCR)</p>
                                <p id="ld-ocr-text" class="text-[12px] text-zinc-700 dark:text-zinc-300 whitespace-pre-wrap break-words">-</p>
                            </div>

                            <div class="lg:col-span-2 rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider mb-2">Resume OCR (RH/Admin)</p>
                                <p id="ld-ocr-summary" class="text-[12px] text-zinc-700 dark:text-zinc-300 whitespace-pre-wrap break-words">-</p>
                            </div>

                            <div class="lg:col-span-2 rounded-lg border border-zinc-100 dark:border-zinc-800 p-4">
                                <p class="text-[11px] font-bold text-zinc-400 uppercase tracking-wider mb-2">Audit Log</p>
                                <pre id="ld-audit" class="text-[12px] text-zinc-700 dark:text-zinc-300 whitespace-pre-wrap break-words">-</pre>
                    </div>
                </div>
            </div>
        </div>
    </div>
</div>

<script>
(function () {
    var modal = document.getElementById('leave-detail-modal');
    if (!modal) return;

    var buttons = document.querySelectorAll('.leave-detail-btn');
    var backdrop = document.getElementById('leave-detail-backdrop');
    var closeBtn = document.getElementById('leave-detail-close');

    var fields = {
        id: document.getElementById('ld-id'),
        employee: document.getElementById('ld-employee'),
        employeeEmail: document.getElementById('ld-employee-email'),
        type: document.getElementById('ld-type'),
        category: document.getElementById('ld-category'),
        status: document.getElementById('ld-status'),
        workflow: document.getElementById('ld-workflow'),
        start: document.getElementById('ld-start'),
        end: document.getElementById('ld-end'),
        days: document.getElementById('ld-days'),
        requestDate: document.getElementById('ld-request-date'),
        returnDate: document.getElementById('ld-return-date'),
        urgency: document.getElementById('ld-urgency'),
        reason: document.getElementById('ld-reason'),
        rhComment: document.getElementById('ld-rh-comment'),
        rhBy: document.getElementById('ld-rh-by'),
        rhAt: document.getElementById('ld-rh-at'),
        adminComment: document.getElementById('ld-admin-comment'),
        adminBy: document.getElementById('ld-admin-by'),
        adminAt: document.getElementById('ld-admin-at'),
        audit: document.getElementById('ld-audit'),
        attachmentLink: document.getElementById('ld-attachment-link'),
        attachmentEmpty: document.getElementById('ld-attachment-empty'),
        ocrText: document.getElementById('ld-ocr-text'),
        ocrSummary: document.getElementById('ld-ocr-summary')
    };

    function read(value, fallback) {
        return value && value.trim() !== '' ? value : fallback;
    }

    function openModal(button) {
        fields.id.textContent = read(button.dataset.id, '-');
        fields.employee.textContent = read(button.dataset.employeeName, '-');
        fields.employeeEmail.textContent = read(button.dataset.employeeEmail, '-');
        fields.type.textContent = read(button.dataset.leaveType, '-');
        fields.category.textContent = read(button.dataset.requestCategory, '-');
        fields.status.textContent = read(button.dataset.status, '-');
        fields.workflow.textContent = read(button.dataset.workflowStatus, '-');
        fields.start.textContent = read(button.dataset.startDate, '-');
        fields.end.textContent = read(button.dataset.endDate, '-');
        fields.days.textContent = read(button.dataset.daysCount, '-');
        fields.requestDate.textContent = read(button.dataset.requestDate, '-');
        fields.returnDate.textContent = read(button.dataset.expectedReturnDate, '-');
        fields.urgency.textContent = read(button.dataset.urgency, '-');
        fields.reason.textContent = read(button.dataset.reason, '-');
        fields.rhComment.textContent = read(button.dataset.rhComment, '-');
        fields.rhBy.textContent = read(button.dataset.rhDecisionBy, '-');
        fields.rhAt.textContent = read(button.dataset.rhDecisionAt, '-');
        fields.adminComment.textContent = read(button.dataset.adminComment, '-');
        fields.adminBy.textContent = read(button.dataset.adminDecisionBy, '-');
        fields.adminAt.textContent = read(button.dataset.adminDecisionAt, '-');
        fields.ocrText.textContent = read(button.dataset.attachmentOcrText, '-');
        fields.ocrSummary.textContent = read(button.dataset.attachmentOcrSummary, '-');
        fields.audit.textContent = read(button.dataset.auditLog, '-');

        var attachment = read(button.dataset.attachmentPath, '');
        if (attachment !== '') {
            fields.attachmentLink.href = attachment;
            fields.attachmentLink.classList.remove('hidden');
            fields.attachmentEmpty.classList.add('hidden');
        } else {
            fields.attachmentLink.href = '#';
            fields.attachmentLink.classList.add('hidden');
            fields.attachmentEmpty.classList.remove('hidden');
        }

        modal.classList.remove('hidden');
        document.body.classList.add('overflow-hidden');
    }

    function closeModal() {
        modal.classList.add('hidden');
        document.body.classList.remove('overflow-hidden');
    }

    buttons.forEach(function (button) {
        button.addEventListener('click', function () {
            openModal(button);
        });
    });

    if (backdrop) backdrop.addEventListener('click', closeModal);
    if (closeBtn) closeBtn.addEventListener('click', closeModal);
    document.addEventListener('keydown', function (event) {
        if (event.key === 'Escape' && !modal.classList.contains('hidden')) {
            closeModal();
        }
    });
})();
</script>

<script>
function decS1(el){el.querySelector('.dec-s0').classList.add('hidden');el.querySelector('.dec-s1').classList.remove('hidden');}
function decS2(el,a){el.querySelector('.dec-s1').classList.add('hidden');var f,label=el.dataset.approveLabel;if(a==='approve'){f=el.querySelector('.dec-s2a');f.action=el.dataset.approveUrl;f.querySelector('[name="_token"]').value=el.dataset.approveToken;f.querySelector('.dec-approve-label').textContent=label;f.classList.remove('hidden');}else{f=el.querySelector('.dec-s2r');f.action=el.dataset.rejectUrl;f.querySelector('[name="_token"]').value=el.dataset.rejectToken;f.classList.remove('hidden');}}
function decReset(el){el.querySelector('.dec-s0').classList.remove('hidden');['dec-s1','dec-s2a','dec-s2r'].forEach(function(c){var n=el.querySelector('.'+c);if(n)n.classList.add('hidden');});}
async function decAi(el,action){
    if(!el){return;}
    var formSelector = action === 'approve' ? '.dec-s2a' : '.dec-s2r';
    var form = el.querySelector(formSelector);
    if(!form){return;}
    var input = form.querySelector('input[name="admin_comment"]');
    var btn = form.querySelector('button[onclick*="decAi"]');
    var notice = form.querySelector('.dec-ai-notice');
    if(!input || !btn){return;}

    var initialLabel = btn.textContent;
    btn.disabled = true;
    btn.textContent = 'Generation...';

    try {
        var response = await fetch(el.dataset.aiUrl, {
            method: 'POST',
            headers: {
                'Content-Type': 'application/json',
                'Accept': 'application/json'
            },
            body: JSON.stringify({
                _token: el.dataset.aiToken,
                leave_id: parseInt(el.dataset.aiLeaveId || '0', 10),
                action: action === 'reject' ? 'REJECT' : 'APPROVE'
            })
        });
        var data = await response.json();
        if(!response.ok || !data.success){
            throw new Error((data && data.message) ? data.message : 'Generation IA indisponible.');
        }

        input.value = data.suggestion || '';
        if(notice){
            notice.textContent = data.notice || 'Texte suggere par IA, a verifier avant envoi.';
            notice.classList.remove('hidden');
        }
    } catch (error) {
        alert(error.message || 'Generation IA indisponible.');
    } finally {
        btn.disabled = false;
        btn.textContent = initialLabel;
    }
}
</script>
{% endblock %}
